sudo: false
language: go
go:
  - 1.6.4
services:
  - mysql
addons:
  apt:
    sources:
      - sourceline: 'ppa:masterminds/glide'
    packages:
      - glide
before_install:
  - test -d $GOPATH/bin || mkdir -p $GOPATH/bin
  - make testsetup
install:
  ## Install dependencies
  - glide install
script:
  - go test
